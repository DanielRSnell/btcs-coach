[deploy]
startCommand = "ls -la public/build/ && cat public/build/manifest.json | grep Dashboard || echo 'Dashboard missing!' && php artisan migrate --force && php artisan db:seed --class=ProductionDataSeeder --force && php artisan serve --host=0.0.0.0 --port=$PORT"