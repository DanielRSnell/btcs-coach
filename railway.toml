[build]
builder = "nixpacks"

[deploy]
startCommand = "npm run build && php artisan config:clear && php artisan cache:clear && php artisan migrate --force && php artisan db:seed --class=ProductionDataSeeder --force && php artisan serve --host=0.0.0.0 --port=$PORT"